services:
  - type: web
    name: car-booking
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 3 --bind 0.0.0.0:5000
    region: singapore
